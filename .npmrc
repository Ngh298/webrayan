# NPM Configuration

# Use legacy peer deps (for compatibility)
legacy-peer-deps=false

# Save exact versions
save-exact=false

# Engine strict
engine-strict=true

# Progress
progress=true

# Audit level
audit-level=moderate

